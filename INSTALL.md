# 🚀 Инструкция по установке и запуску
## Hotel Management System by Li Nikita

---

## ⚡ Быстрый старт (для Windows)

### 1. Проверьте Python
Откройте командную строку (cmd) и выполните:
```cmd
python --version
```
Должна быть версия Python 3.8 или выше. Если Python не установлен, скачайте с [python.org](https://python.org)

### 2. Перейдите в папку проекта
```cmd
cd hotel_system_li_nikita
```

### 3. Установите зависимости
```cmd
pip install flask
```

### 4. Запустите систему
```cmd
python app.py
```

### 5. Откройте в браузере
```
http://localhost:5000
```

---

## 🔧 Подробная установка

### Системные требования
- **ОС**: Windows 10/11, macOS, Linux
- **Python**: 3.8+
- **Браузер**: Chrome, Firefox, Safari, Edge (современные версии)
- **Память**: минимум 512 МБ RAM
- **Место на диске**: 50 МБ

### Установка Python (если не установлен)

#### Windows:
1. Скачайте Python с [python.org](https://python.org)
2. Запустите установщик
3. ✅ Обязательно отметьте "Add Python to PATH"
4. Нажмите "Install Now"

#### macOS:
```bash
# Через Homebrew
brew install python3

# Или скачайте с python.org
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip
```

### Установка зависимостей

#### Способ 1 (рекомендуемый):
```cmd
pip install -r requirements.txt
```

#### Способ 2 (ручная установка):
```cmd
pip install flask
```

---

## 🎯 Первый запуск

### 1. Запуск сервера
```cmd
python app.py
```

Вы увидите:
```
* Running on http://127.0.0.1:5000
* Debug mode: on
```

### 2. Открытие в браузере
Перейдите по адресу: **http://localhost:5000**

### 3. Первоначальная настройка
При первом запуске система автоматически:
- ✅ Создаст базу данных `hotel_system.db`
- ✅ Добавит тестовые номера
- ✅ Настроит все необходимые таблицы

---

## 📱 Тестирование функций

### Проверьте основные функции:

1. **Дашборд** - главная страница со статистикой
2. **Номера** - управление статусами номеров
3. **Гости** - заселение/выселение
4. **Задачи** - создание и выполнение задач
5. **Качество** - проверки качества обслуживания

### Тестовые данные:
- 5 номеров разных типов (101, 102, 201, 202, 301)
- Различные статусы для демонстрации

---

## 🔧 Настройка для продакшена

### Изменение настроек безопасности:
В файле `app.py` измените:
```python
app.secret_key = 'your-secret-key-here'  # Смените на уникальный ключ
app.run(debug=False, host='0.0.0.0', port=5000)  # Отключите debug
```

### Использование с внешним сервером:
```bash
# Установите gunicorn
pip install gunicorn

# Запустите
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

---

## 🚨 Решение проблем

### Проблема: "python не найден"
**Решение**: Установите Python или добавьте в PATH

### Проблема: "pip не найден"
**Решение**: 
```cmd
python -m pip install flask
```

### Проблема: "Порт 5000 занят"
**Решение**: Измените порт в `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=8080)  # Используйте другой порт
```

### Проблема: "База данных заблокирована"
**Решение**: Закройте все экземпляры приложения и перезапустите

### Проблема: "Стили не загружаются"
**Решение**: Проверьте подключение к интернету (используется Tailwind CDN)

---

## 📊 Структура файлов после установки

```
hotel_system_li_nikita/
├── 📄 app.py                 # Основное приложение
├── 📄 requirements.txt       # Зависимости
├── 📄 README.md             # Документация
├── 📄 INSTALL.md            # Эта инструкция
├── 🗃️ hotel_system.db       # База данных (создается автоматически)
├── 📁 templates/            # HTML шаблоны
├── 📁 static/              # CSS, JS, изображения
└── 📁 __pycache__/         # Кэш Python (создается автоматически)
```

---

## 🎯 Готово к использованию!

После успешной установки система готова к работе:

✅ **Администраторы** могут использовать полный функционал на ПК  
✅ **Персонал** может работать через мобильные устройства  
✅ **Автоматическое** создание задач при выселении гостей  
✅ **Контроль качества** с системой оценок  

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте версию Python: `python --version`
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порт 5000 свободен
4. Перезапустите приложение

**Разработчик**: Ли Никита  
**© 2024 Hotel Management System**